<Window x:Class="StocksDisplay.View.DetailedCompanyView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:oxy="http://oxyplot.org/wpf"
        xmlns:local="clr-namespace:StocksDisplay.View"
        mc:Ignorable="d"
        Title="DetailedCompanyView" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0" Margin="10">
            <Image x:Name="CompanyLogo" Width="50" Height="50" />
            <TextBlock x:Name="CompanyName" FontWeight="Bold" VerticalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" Margin="0,10,0,0"/>
        </StackPanel>

        <StackPanel Grid.Column="1" Margin="10">
            <ComboBox Name="ChartOptions" Width="200" Margin="0,0,0,10"></ComboBox>
            <Button Content="Show Chart" Width="100" Click="ShowChart_Click"/>
        </StackPanel>

        <oxy:PlotView x:Name="CompanyDataChart" Margin="10,116,10,0" Grid.RowSpan="3" Grid.ColumnSpan="3"/>
    </Grid>
</Window>
