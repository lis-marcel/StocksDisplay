<Window x:Class="StocksDisplay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

        xmlns:converters="clr-namespace:StocksDisplay.Converters"
        
        mc:Ignorable="d"
        Title="Stocks Display" Height="295" Width="200">

    <Window.Resources>
        <converters:IsPositiveConverter x:Key="IsPositiveConverter"/>
        
        <converters:TickerToImagePathConverter x:Key="TickerToImagePathConverter"/>
    </Window.Resources>
    <Grid>
        <ItemsControl ItemsSource="{Binding StocksList}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                            <GradientStop Color="Red" Offset="0.0"/>
                                            <GradientStop Color="LightCoral" Offset="1.0"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding PercentageChange, Converter={StaticResource IsPositiveConverter}}" Value="True">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                                    <GradientStop Color="Green" Offset="0.0"/>
                                                    <GradientStop Color="LightGreen" Offset="1.0"/>
                                                </LinearGradientBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

    </Grid>
</Window>
